# Reference clones — study/mine only, NEVER committed
refs/

# Python bytecode cache
__pycache__/
*.pyc
*.pyo

# Logs / local artifacts
*.log
_legacy/

# Self-improving skills: learned drafts + runtime state (kept out of VCS until reviewed)
skills/learned/
data/skills_feedback.jsonl
data/skills_tidy_report.md
data/skills_vectors.json

# Large model weights (downloaded at runtime via `glados download`)
*.onnx
*.bin
*.nemo
*.gguf
*.pt
*.safetensors

# Local development docs & tooling (kept out of the published repo)
*CLAUDE.md
docs/*
MD/*
.claude/

# Local secrets (API tokens etc.) — copy .env.example to .env and fill in real values
.env
